# shortest-path-bfs-grid
A Java project that finds the shortest path between two points in a grid using BFS. The grid is treated as a graph where each cell is a node and moves are allowed in four directions. It handles blocked cells, user input, and also prints the actual shortest path
